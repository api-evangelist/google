naftiko: 1.0.0-alpha2
info:
  label: Google Gmail API — Users
  description: 'Google Gmail API — Users. 2 operations. Lead operation: Google Stop Notifications for User. Self-contained
    Naftiko capability covering one Google business surface.'
  tags:
  - Google
  - Users
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GOOGLE_API_KEY: GOOGLE_API_KEY
capability:
  consumes:
  - type: http
    namespace: gmail-users
    baseUri: https://gmail.googleapis.com
    description: Google Gmail API — Users business capability. Self-contained, no shared references.
    resources:
    - name: gmail-v1-users-userId-stop
      path: /gmail/v1/users/{userId}/stop
      operations:
      - name: post
        method: POST
        description: Google Stop Notifications for User
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: userId
          in: path
          type: string
          description: (Required)
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    - name: gmail-v1-users-userId-watch
      path: /gmail/v1/users/{userId}/watch
      operations:
      - name: post
        method: POST
        description: Google Watch Users
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Content-Type
          in: header
          type: string
        - name: Accept
          in: header
          type: string
        - name: userId
          in: path
          type: string
          description: (Required)
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
  exposes:
  - type: rest
    namespace: gmail-users-rest
    port: 8080
    description: REST adapter for Google Gmail API — Users. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/gmail/v1/users/{userid}/stop
      name: gmail-v1-users-userid-stop
      description: REST surface for gmail-v1-users-userId-stop.
      operations:
      - method: POST
        name: post
        description: Google Stop Notifications for User
        call: gmail-users.post
        with:
          userId: rest.userId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/gmail/v1/users/{userid}/watch
      name: gmail-v1-users-userid-watch
      description: REST surface for gmail-v1-users-userId-watch.
      operations:
      - method: POST
        name: post
        description: Google Watch Users
        call: gmail-users.post
        with:
          Content-Type: rest.Content-Type
          Accept: rest.Accept
          userId: rest.userId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: gmail-users-mcp
    port: 9090
    transport: http
    description: MCP adapter for Google Gmail API — Users. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: google-stop-notifications-user
      description: Google Stop Notifications for User
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: gmail-users.post
      with:
        userId: tools.userId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: google-watch-users
      description: Google Watch Users
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: gmail-users.post
      with:
        Content-Type: tools.Content-Type
        Accept: tools.Accept
        userId: tools.userId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
