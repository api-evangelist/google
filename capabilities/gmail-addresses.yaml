naftiko: 1.0.0-alpha2
info:
  label: Google Gmail API — Addresses
  description: 'Google Gmail API — Addresses. 3 operations. Lead operation: Google List Forwarding Addresses. Self-contained
    Naftiko capability covering one Google business surface.'
  tags:
  - Google
  - Addresses
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GOOGLE_API_KEY: GOOGLE_API_KEY
capability:
  consumes:
  - type: http
    namespace: gmail-addresses
    baseUri: https://gmail.googleapis.com
    description: Google Gmail API — Addresses business capability. Self-contained, no shared references.
    resources:
    - name: gmail-v1-users-userId-settings-forwardingAddresses
      path: /gmail/v1/users/{userId}/settings/forwardingAddresses
      operations:
      - name: get
        method: GET
        description: Google List Forwarding Addresses
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Accept
          in: header
          type: string
        - name: userId
          in: path
          type: string
          description: (Required)
          required: true
    - name: gmail-v1-users-userId-settings-forwardingAddresses-forwardingEmail
      path: /gmail/v1/users/{userId}/settings/forwardingAddresses/{forwardingEmail}
      operations:
      - name: get
        method: GET
        description: Google Get Forwarding Addresses
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Accept
          in: header
          type: string
        - name: userId
          in: path
          type: string
          description: (Required)
          required: true
        - name: forwardingEmail
          in: path
          type: string
          description: (Required)
          required: true
      - name: delete
        method: DELETE
        description: Google Delete Forwarding Addresses
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: userId
          in: path
          type: string
          description: (Required)
          required: true
        - name: forwardingEmail
          in: path
          type: string
          description: (Required)
          required: true
  exposes:
  - type: rest
    namespace: gmail-addresses-rest
    port: 8080
    description: REST adapter for Google Gmail API — Addresses. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/gmail/v1/users/{userid}/settings/forwardingaddresses
      name: gmail-v1-users-userid-settings-forwardingaddresses
      description: REST surface for gmail-v1-users-userId-settings-forwardingAddresses.
      operations:
      - method: GET
        name: get
        description: Google List Forwarding Addresses
        call: gmail-addresses.get
        with:
          Accept: rest.Accept
          userId: rest.userId
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/gmail/v1/users/{userid}/settings/forwardingaddresses/{forwardingemail}
      name: gmail-v1-users-userid-settings-forwardingaddresses-forwardingemail
      description: REST surface for gmail-v1-users-userId-settings-forwardingAddresses-forwardingEmail.
      operations:
      - method: GET
        name: get
        description: Google Get Forwarding Addresses
        call: gmail-addresses.get
        with:
          Accept: rest.Accept
          userId: rest.userId
          forwardingEmail: rest.forwardingEmail
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: delete
        description: Google Delete Forwarding Addresses
        call: gmail-addresses.delete
        with:
          userId: rest.userId
          forwardingEmail: rest.forwardingEmail
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: gmail-addresses-mcp
    port: 9090
    transport: http
    description: MCP adapter for Google Gmail API — Addresses. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: google-list-forwarding-addresses
      description: Google List Forwarding Addresses
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: gmail-addresses.get
      with:
        Accept: tools.Accept
        userId: tools.userId
      outputParameters:
      - type: object
        mapping: $.
    - name: google-get-forwarding-addresses
      description: Google Get Forwarding Addresses
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: gmail-addresses.get
      with:
        Accept: tools.Accept
        userId: tools.userId
        forwardingEmail: tools.forwardingEmail
      outputParameters:
      - type: object
        mapping: $.
    - name: google-delete-forwarding-addresses
      description: Google Delete Forwarding Addresses
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: gmail-addresses.delete
      with:
        userId: tools.userId
        forwardingEmail: tools.forwardingEmail
      outputParameters:
      - type: object
        mapping: $.
